name: Forest Silhouette One Shot

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/forest-silhouette-one-shot.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply forest silhouette refinement
        run: python scripts/tmp_forest_silhouette.py
      - run: npm run build
      - run: git diff --check
      - name: Commit forest refinement and remove temporary files
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/rendering/generated-world-render-bridge.ts src/rendering/environment-detail-layer.ts
          git rm scripts/tmp_forest_silhouette.py .github/workflows/forest-silhouette-one-shot.yml
          git commit -m 'visual: refine forest silhouettes and canopy integration'
          git push
