name: Strategic Site Context One Shot

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/site-context-one-shot.yml'

permissions:
  contents: write
  actions: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply presentation-only strategic site context pass
        run: python scripts/tmp_site_context_patch.py
      - run: npm run build
      - run: git diff --check
      - name: Commit visual polish and remove temporary files
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/rendering/environment-detail-layer.ts src/rendering/poi-render-bridge.ts src/rendering/resource-render-bridge.ts
          git rm scripts/tmp_site_context_patch.py .github/workflows/site-context-one-shot.yml
          git commit -m 'visual: enrich settlement resource and POI context'
          git push
      - name: Deploy final main
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sleep 3
          gh workflow run deploy-pages.yml --ref main
